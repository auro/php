stages:
  - prepare
  - build
  - release
  - deploy
  - undeploy

variables:
  domain: merco.net
  project: lamp

Prepare:
  stage: prepare
  tags:
    - docker
  before_script:
    - echo Setting up $project.$domain
    - uptime
    - whoami
    - uname -a
    - docker --version
    - docker-compose --version
    - docker-machine --version
  script:
    - ps aux
  only:
    - master

Build:
  stage: build
  tags:
    - docker
  script:
    - docker-compose build
  only:
    - master

Release:
  stage: release 
  tags:
    - docker
  before_script:
    - docker login -u gitlab-ci-token -p $CI_JOB_TOKEN $CI_REGISTRY
  script:
    - docker-compose build
  only:
    - master

Deploy:
  stage: deploy
  tags:
    - docker
  script:
    - docker-compose up -d
  only:
    - master
  when: manual

Undeploy:
  stage: undeploy
  tags:
    - docker
  script:
    - docker-compose down --volumes --remove-orphans
  only:
    - master
  when: manual

